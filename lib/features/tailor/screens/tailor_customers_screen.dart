import 'package:flutter/material.dart';

class TailorCustomersScreen extends StatelessWidget {
  const TailorCustomersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Customers",
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
