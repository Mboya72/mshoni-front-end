import 'package:flutter/material.dart';

class TailorMessageScreen extends StatelessWidget {
  const TailorMessageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Messages",
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
